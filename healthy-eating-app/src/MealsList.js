import Counter from "./Counter";
const MealsList = () =>{
    const todaysMeals = [
        {name: "Baked Beans", calories: 200, id: 12345},
        {name: "Grilled Veggies", calories: 80, id: 23456},
        {name: "Soup", calories: 100, id: 34567}
    ]
    const[meals, setMeals] = react.useState(todaysMeals);

    function handleEvent(){
        <Counter clikRate = {`${word}`}/>
    }
    <div>
        <button onClick={handleEvent}>Eat your Meal!</button>
    </div>
}

export default MealsList;